@model IEnumerable<Bio>
<div class="container">
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Bio Table</h4>
                    <p class="card-description">
                        <a asp-controller="Bios" asp-action="create" class="btn btn-outline-success">Create Bios</a>
                    </p>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>

                                    <th>Key</th>
                                    <th>Value</th>
                                    <th>Settings</th>


                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var bio in Model)
                                {
                                    <tr>

                                        <td>@bio.Key</td>
                                        <td>@bio.ShortValue</td>

                                        <td>
                                            <a asp-action="update" asp-route-id="@bio.Id" class="btn btn-outline-info"><i class="ni ni-ruler-pencil"></i></a>
                                            <a asp-action="delete" asp-route-id="@bio.Id" class="btn btn-outline-secondary"><i class="ni ni-fat-remove"></i></a>
                                            <a asp-action="detail" asp-route-id="@bio.Id" class="btn btn-outline-behance"><i class="ni ni-settings-gear-65"></i></a>
                                        </td>

                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>