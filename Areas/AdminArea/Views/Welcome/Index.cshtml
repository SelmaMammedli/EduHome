@model IEnumerable<Welcome>
<div class="container">
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Welcome Table</h4>
                    <p class="card-description">
                        <a asp-controller="Welcome" asp-action="create" class="btn btn-outline-success">Create Welcome part</a>
                    </p>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Image</th>
                                    <th>Title</th>
                                    <th>Description</th>
                                    <th>VideoLink</th>
                                    <th>Settings</th>


                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var wel in Model)
                                {
                                    <tr>
                                        <td>
                                            <img width="200" height="100" src="~/img/about/@wel.ImageUrl" alt="" />
                                        </td>
                                        <td>@wel.Title</td>
                                        <td>@wel.ShortDesc</td>
                                        <td>@wel.VideoLink</td>
                                        <td>
                                            <a asp-action="update" asp-route-id="@wel.Id" class="btn btn-outline-info"><i class="ni ni-ruler-pencil"></i></a>
                                            <a asp-action="delete" asp-route-id="@wel.Id" class="btn btn-outline-secondary"><i class="ni ni-fat-remove"></i></a>
                                            <a asp-action="detail" asp-route-id="@wel.Id" class="btn btn-outline-behance"><i class="ni ni-settings-gear-65"></i></a>
                                        </td>

                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
